# Newsletter

Outputs headlines from major news sources such as the NYTimes and FoxNews into a text file. The headlines are then sent to the specified email address.

On the command line, run:

```
./main <email@address> <output.txt> <optional keyword>
```

Feel free to add websites to listOfWebsites.
